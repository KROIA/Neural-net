import QtQuick 2.0
import QtCharts 2.3

Item {

    /*ChartView {
        title: "Line"
        anchors.fill: parent
        antialiasing: true
        model:
        LineSeries {

            Repeater{
                model: ui.errorChart.length
                XYPoint {
                    x: {consol.debug(index+" "+ui.errorChart[index])
                                    return index;

                    }

                    y: ui.errorChart[index]

                }

            }
        }
    }*/
}
